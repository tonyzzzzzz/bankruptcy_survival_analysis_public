# Surival Analysis of Bankruptcy

- all experiment code goes into `bsa`. 
- all data goes into `data`
- all dev scripts go into `/dev`. all files should begin as `dev_*.py`
- all jupyter notebooks go into `/scripts`.

 
